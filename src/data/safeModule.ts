const createEnableModuleParams = [
  {
    key: "accountAddress",
    type: "string",
    description: "The target Safe Account address to enable the module for.",
  },
];

export { createEnableModuleParams };
